@patch('network_module.get_latency')\ndef test_high_latency_warning(mock_get_latency):\n    \"\"\"\n    Test if a warning is raised when latency exceeds the acceptable threshold.\n    \"\"\"\n    # Arrange\n    mock_get_latency.return_value = 55  # Mock latency value\n\n    # Act\n    with pytest.raises(Warning):\n        if network_module.get_latency() > 50:\n            raise Warning("Latency too high!")\n\n    # Assert\n    # Exception expected, handled by pytest.raises