import pytest\nfrom fastapi.testclient import TestClient\nfrom app import app  # Assuming 'app' is your FastAPI instance\n\nclient = TestClient(app)\n\n@pytest.mark.asyncio\ndef test_swipe_left_menu_navigation():\n    response = client.post('/swipe', json={'direction': 'left'})\n    assert response.status_code == 200, f"Expected status code 200, got {response.status_code}"\n    data = response.json()\n    assert 'previous_menu' in data, "Response does not contain 'previous_menu' key"