import pytest\nfrom fastapi.testclient import TestClient\nfrom app import app\n\nclient = TestClient(app)\n\n@pytest.mark.asyncio\ndef test_swipe_right_menu_navigation():\n    response = client.post('/swipe', json={'direction': 'right'})\n    assert response.status_code == 200, f"Expected status code 200, got {response.status_code}"\n    data = response.json()\n    assert 'next_menu' in data, "Response does not contain 'next_menu' key"