import pytest\nfrom fastapi.testclient import TestClient\nfrom app import app\n\nclient = TestClient(app)\n\n@pytest.mark.asyncio\ndef test_swipe_up_open_settings():\n    response = client.post('/swipe', json={'direction': 'up'})\n    assert response.status_code == 200, f"Expected status code 200, got {response.status_code}"\n    data = response.json()\n    assert 'settings_opened' in data, "Response does not contain 'settings_opened' key"