@patch('network_module.send_packet')\ndef test_reliable_data_packet_transmission(mock_send_packet):\n    \"\"\"\n    Test if data packets are transmitted reliably under normal conditions.\n    \"\"\"\n    # Arrange\n    mock_send_packet.return_value = True\n    packet = {'data': 'test_data'}\n\n    # Act\n    result = network_module.send_packet(packet)\n\n    # Assert\n    assert result is True, "Expected packet transmission to be successful"