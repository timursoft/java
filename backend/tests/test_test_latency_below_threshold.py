import pytest\nfrom unittest.mock import Mock, patch\n\n@patch('network_module.get_latency')\ndef test_latency_below_threshold(mock_get_latency):\n    \"\"\"\n    Test if latency stays consistently below 50ms under normal conditions.\n    \"\"\"\n    # Arrange\n    mock_get_latency.return_value = 45  # Mock latency value\n\n    # Act\n    latency = network_module.get_latency()\n\n    # Assert\n    assert latency < 50, f"Expected latency to be below 50ms, but got {latency}ms"